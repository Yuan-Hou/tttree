# 注:各供应商的模型 API key(DEEPSEEK_API_KEY / OPENAI_API_KEY / CLAUDE_API_KEY / ZAI_API_KEY /
# GOOGLE_API_KEY)已不再由本服务使用——「本站点服务」改走下方的 new-api 网关、按用户独立 token。
# 这些 key 现配在 new-api 的渠道里;即便残留在 .env 也会被忽略,不必填。

# 对称加密 + 登录令牌签名共用的主密钥。两处用途:
#   1) 全局设置里「自填供应商 API key」落库前的对称加密(Fernet);不配则只能用「本站点服务」。
#   2) 用户系统的登录令牌(JWT,HMAC-SHA256)签名/校验;不配则**无法登录**。
# 必须配置,且建议 ≥32 字节随机串,例如 `openssl rand -hex 32`。
APP_SECRET=change-me-to-a-long-random-string

# 用户系统:DB 表为真相源。首次启动从 backend/auth_users.toml(gitignored,见 auth_users.example.toml)
# 种子出管理员;之后增删改用户走管理控制台(管理员登录后进入),不再依赖该文件。

# 站点品牌名:界面展示为「{SITE_NAME} Tree」。留空 → 仅「Tree」。前端从 /brand 同步后本地缓存。
SITE_NAME=

# new-api 网关:「本站点服务」经它、按每用户独立 token 调模型(用户无权用上面的官方供应商 key)。
# NEW_API_BASE_URL  自建 new-api 站点地址(默认即指向它)。
# NEW_API_ADMIN_KEY root 用户的 access token(站点管理用,非模型 key);新用户登录时据此自动建子用户+取模型 key。
# NEW_API_ADMIN_USER_ID  管理 API 必带的 New-Api-User 头(管理员数字 id,root 通常为 1)。
NEW_API_BASE_URL=https://api.tttree.online
NEW_API_ADMIN_KEY=
NEW_API_ADMIN_USER_ID=1